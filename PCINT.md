# PC-インタープリタ

「PC-インタープリタ」(PC-1245/51/55用)の文法で書いたプログラムが動きます。
(PC-インタープリタと同じ文法をPythonで読んで実行しているだけですので、
機械語を含むプログラムは動きません。)
出典は工学社「PiO」1986年8月号 169〜171ページ、作者は
[Fan_PC-1251](https://x.com/pio1986_10)氏です。

```sh
uv run play.py pcint
```

`programs/pcint/progs/`にある`.pci`を左右のキーで選び、SPACEで実行します。
メニューからは「5 PC-INT」です。



## 文法

定数は16進2ケタ、変数はA〜Zの1バイト、配列は(&00)〜(&9F)です。A〜Zは
配列の&86〜&9Fと同じ場所にあるので、Aは&C686、Zは&C69Fになります。式は
前から順に計算し、カッコはありません。桁あふれは切り捨てます。

| 命令 | 働き |
| --- | --- |
| `CLEAR` | 変数と配列を全部0にする |
| `IF 式 関係 式` | 満たさなければ次の行へ。関係は `=` `>` `<` `<>` |
| `NOT` | IFの実行と非実行を入れ替える |
| `GOTO 式` | 式の16進2ケタを行番号の下2ケタとして飛ぶ |
| `FOR 式` 〜 `NEXT` | 式の回数だけ繰り返す。1段だけ |
| `PRINT "文字列"` | CALLで決めた桁から表示する |
| `CALL 式` | 次のPRINTの桁を決める |
| `COS` | 画面を消す |
| `SQR` | 画面を白黒反転する |
| `DEG 位置,データ…` | 位置から列データを並べる |
| `BEEP 式1,式2` | 式1の高さで式2の長さだけ鳴らす。鳴り終わるまで止まる |
| `WAIT 式` | 式/256秒だけ待つ |
| `STOP` | キーが押されるまで止まる |
| `PEEK 式` | その番地の内容 |
| `INKEY$` | 押しているキーのコード。何も押していなければ&FF |
| `RND 式` | 0〜式-1の乱数 |
| `LET` | 次の命令を無視する(オリジナルのとおり) |
| `END` | 終わる |
| `.` | 以降を読まない |

`&`と`=`と`:`はオリジナルのとおり省けます。省いた字面は読む順序で
決めています。
命令の頭ではまず命令語、次に1文字の変数として読み、値のところでは16進2ケタ
が並んでいればそれを定数として読みます。`FF`は定数`&FF`、`GG+01`は変数Gに
1を加える代入`G=G+&01`になるのはこの規則によります。

```
11 IF G<FFGG+01          IF G<&FF THEN G=G+&01 と同じ
35 BMWG/1E               B=M : W=G/&1E と同じ
```

## 本物とはちがうところ

- **BEEPの換算**: オリジナルには「値が大きいと高い(長い)音」としか書いて
  いないので、周波数は`120 + 式1*28`Hz(6000Hzで頭打ち)、長さは`式2*8`ミリ秒
  (最短8ミリ秒)としました。`interp.py`の`beep_hz`と`beep_ms`にあります。
- **PEEKの範囲**: 式が1バイトなので、読めるのは配列の&00〜&9Fだけです。
- **COM$**: ユーザーのマシン語を&C5D0で呼ぶ命令なので、何もしません。
- **キー**: 表2のコードをそのまま使います。数字は&40〜&49、英字は&51〜&6A、
  ENTERが&00、BRK(ESC)が&07、矢印が&0C〜&0Fです。矢印にはWASDも割り当てて
  あるので、AやSは矢印として読まれます。SPCは表2に載っていないので、英字の
  次の&6Bを当てました。

## .pciの書き方

行番号のあとに命令を並べます。

オリジナルではこのプログラムをBASICの行として打ち込み、処理系がそれを独自の
文法規則に従って読み直して走らせていました。起動と後始末はBASICで書くので、
リストの末尾にはBASICの行が付きます。オリジナルの例では900行がそれで、
`CALL &C300`で処理系を呼び、戻ってきたら`PEEK`で得点を表示していました。
ここではその行を実行できないので、900番以上の行は読み飛ばします。代わりに、
先頭へ次の見出しを書けます。

```
#TITLE JUMP                メニューに出す名前
#SCORE &C698               終わったときに表示する得点の番地
#SCORE &C69A,&C699         2つ書くと 前*100+後 の1つの数として出す
#SPEED 12                  1フレーム(1/24秒)に進める命令数。既定は60
#POKE &14 3E,51,49,45,3E   BASICがCALL前にPOKEしていたぶんのデータ
```

変数は1バイトなので255までしか入りません。それより大きい得点を出すには、
下2桁の変数が&63(99)を超えたら上の変数へ1くり上げる、といった書き方を
します。`#SCORE`に番地を2つ並べるのはそのためで、上の桁の番地から順に
書きます。

同梱しているのは`dot.pci`(点が跳ねまわる)、`wave.pci`(波を1列ずつ描く)、
`jump.pci`(SPCで跳んでよける)の3本です。

`WAIT`や`BEEP`で処理に待ち時間が入る場合を除き、`#SPEED`で速さを調整します。
既定の60は1秒に1440命令で、実機のBASICの5〜7倍というオリジナルの記述に
合わせたつもりですが、あまりうまく調整できていないようです。かなり速く
感じることがあります。
`wave.pci`は20(1秒に480命令)にしてあります。

`#`で始まるその他の行はコメントとして読み飛ばします。

雑誌に載っていたプログラムは著作権の問題もありますので入れていませんが、
文法は同じなので、`.pci`に書き写せばそのまま動きます。

## エラー

オリジナルと同じく1、2、4の番号でエラーを返します。実行を止めて
`ERROR 1 IN 15`のように出します。

| 番号 | 意味 |
| --- | --- |
| 1 | 文法エラー |
| 2 | 演算エラー(0で割った) |
| 4 | ラインエラー(GOTOの先が無い) |

BRK(ESC)を押すと止まります。
