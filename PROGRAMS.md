# programs/ の書き方

`programs/`に置くモジュールは、`pc1251`パッケージが提供する`Machine`と
`Buffer`だけを使って書きます。pygameもPillow(PIL)もimportしません。画面・
入力・時間をすべて`Machine`経由にしてあるので、表示側を差し替えてもゲームは
そのまま動きます。

動くものを先に見たいなら`examples/`に短い例が入っています。
`uv run play.py hello`から順にどうぞ。

Pythonのかわりに、雑誌に載った「PC-インタープリタ」の文法でも書けます。
書き方は[PCINT.md](PCINT.md)にあります。

## 最小のプログラム

`main(m)`を持つモジュールを置けば`play.py <モジュール名>`で起動できます。

```python
# programs/hello.py
def main(m):
    x = 0
    while m.tick():
        m.cls()
        m.text(x, "HELLO")
        x = (x + 1) % m.WIDTH
        if m.pressed("BRK"):
            return
```

```sh
uv run play.py hello
```

`m.tick()`が1フレームの区切りです。今バッファに描いてある内容を液晶へ出し、
次のフレームまで待って、キーの状態を更新します。ウィンドウが閉じられると
`False`を返すので、そのときは素直に`return`してください。`main`から抜けると
ランチャーに戻ります(直接起動していれば終了します)。

メニューに載せるには`programs/menu.py`の`TITLES`に1行足します。

## 画面

描けるのは **120x7ドット**。5x7ドットの文字セルが24個ならんだもの(実機の
言い方では24桁)で、セルとセルのあいだの1ドットは物理的に存在しません。

座標はすべてドット単位です。左上が(0, 0)、右下が(119, 6)になります。

画面は1ビットなので、重なりは描いた順で決まります。後から描いたものが勝ち
ます。背景の濃い場所にスプライトを置いて埋もれる場合は、先に縁を消します。

```python
m.erase(x, y, 6, 3, margin=1)   # 1ドットの白フチを作る
m.sprite(x, y, HERO)
```

スコアの表示欄は、ゲームの絵がはみ出してこないよう、その範囲を毎フレーム
0で潰してから数字を置きます。

```python
for y in range(m.HEIGHT):
    for x in range(96, m.WIDTH):
        m.px(x, y, False)
m.text(96, f"{score:04d}")
```

## 仮想画面(Buffer)

画面は縦7ドットしかありませんが、`Buffer(h)`を使えば好きな高さのドットの
入れ物を作れます。これを仮想画面と呼ぶことにします。`Machine`と同じ描き方
(`px` `hline` `box` `sprite` `text`など)ができるので、書き方は変わりません。

そこへ大きい絵を描いておいて、`m.show(buf, top)`で縦7ドットぶんだけ
切り取って画面に写します。`top`が切り取る位置です。

```
   仮想画面 Buffer(30)              画面 (縦7ドット)
 y= 0 +--------------------+
      |                    |
 y=10 |====================|  <-- m.show(buf, 10) はここから
      |                    |         7ドットぶんを         +----------+
      |                    |         切り取って写す  --->  |          |
 y=16 |====================|                               +----------+
      |                    |
 y=29 +--------------------+
```

`top`を毎フレーム少しずつ変えると、仮想画面の上を窓が動いていくように
見えます。窓を動かしているだけなので、仮想画面のほうは1回描けば済みます。

```python
from pc1251 import Buffer

vscr = Buffer(30)          # 縦30ドットの仮想画面
vscr.cls()
vscr.outline(10, 0, 44, 29)

top = 0.0
while m.tick():
    top += 0.4             # 窓を下へずらしていく
    m.cls()
    m.show(vscr, int(round(top)))
```

`top`は負でも仮想画面の高さを超えていてもかまいません。はみ出したところの
ドットは消えた状態になります。

窓の動かし方には気をつけてください。`examples/tall.py`では0.5ドット/フレーム
にしてあります。1ドットずつ動かすと同じ位置が1フレームしか映らず、液晶の
残像で尾を引いて何も読めなくなります。

## MachineのAPI

### 描画

座標はドット単位です。左上が(0, 0)、右下が(119, 6)。画面の外へはみ出した
部分は黙って捨てられるので、範囲を気にせず書いてかまいません。線や四角は
両端を含みます。

**`m.cls()`**
画面のドットを全部消します。ふつうは毎フレームの描き始めに呼びます。

**`m.px(x, y, on=True)`**
(x, y)のドットを1つ点けます。`on=False`を渡すと消します。

**`m.get(x, y)`**
(x, y)のドットが点いていれば1、消えていれば0を返します。画面の外は0です。
当たり判定には使わないでください(「当たり判定」の節)。

**`m.hline(x0, x1, y, on=True)`**
yの行に、x0からx1までの横線を引きます。両端を含むので`m.hline(0, 4, 3)`は
x=0,1,2,3,4の5ドットになります。x0がx1より大きいと1ドットも引かれません。

**`m.vline(x, y0, y1, on=True)`**
xの列に、y0からy1までの縦線を引きます。同じく両端を含みます。
横線はセルの境目で切れて見えますが、縦線は境目をまたがないので切れません。

**`m.box(x0, y0, x1, y1, on=True)`**
左上(x0, y0)から右下(x1, y1)までの四角を塗りつぶします。枠のドットも含みます。

**`m.outline(x0, y0, x1, y1, on=True)`**
同じ四角の枠だけを引きます。中は触らないので、下に描いてあるものが残ります。

**`m.sprite(x, y, rows, on=True)`**
ドット絵を置きます。左上が(x, y)です。渡し方は下に書きました。

**`m.erase(x, y, w, h, margin=0)`**
(x, y)から幅w・高さhの範囲を消します。`margin`を付けると外側にもそのぶん
広げて消すので、絵のまわりに白いフチができます。

**`m.text(x, s, y=0, scale=1)`**
xから文字列sを書きます。1文字5ドット幅で詰めて並ぶので、5文字で25ドットです。
yは文字の上端。`scale=2`にすると1文字10x14ドットになりますが、画面は
縦7ドットなので入りません。縦に高い`Buffer`と組み合わせて使います。

**`m.cell_text(cell, s)`**
`m.text`と同じですが、位置を文字セルの番号(0〜23)で指定します。
`m.cell_text(3, "AB")`は`m.text(15, "AB")`と同じです。セルにそろえたいときは
こちらが早いです。

**`m.gprint(x, cols)`**
縦1列ぶんの数を並べて描きます。`cols`の1要素が縦7ドットで、bit0が上端、
bit6が下端です。`m.gprint(0, [0b0000001, 0b1111111])`ならx=0の列は上端だけ、
x=1の列は7ドット全部が点きます。地形の高さのような、列ごとの形に向きます。

**`m.invert(x0=0, y0=0, x1=119, y1=6)`**
その範囲の点灯と消灯を入れ替えます。省略すると画面全体です。2回かければ
元に戻るので、やられたときの点滅などに使います。

**`m.show(buf, top)`**
仮想画面`buf`のy=topから縦7ドットぶんを切り取って、画面へ写します。
`top`は負でも仮想画面の高さを超えていてもよく、はみ出したところのドットは
消えた状態になります。
「仮想画面(Buffer)」の節を参照してください。

**`m.symbol(name, on)`**
液晶の上に並んでいるインジケータを点けたり消したりします。名前は
`BUSY` `DEF` `SHIFT` `HYP` `RUN` `PRO` `RSV` `DEG` `RAD` `GRAD` `E`。
起動したときは`RUN`と`DEG`が点いています。

どれも`on=False`を渡せば、その形のとおりに消せます。
`m.box(10, 0, 20, 6, False)`ならその範囲だけ白く抜けます。

### 入力

**`m.key(name)`**
そのキーがいま押されていればTrueです。押しているあいだずっとTrueになります。
移動のように、押し続けたいものに使います。

**`m.pressed(name)`**
そのキーがこのフレームで押されたらTrueです。押した瞬間の1フレームだけ。
押しっぱなしでも連射にならないので、決定やジャンプに使います。

**`m.any_pressed()`**
どれかのキーがこのフレームで押されたらTrueです。「何か押すと戻る」に
使います。

**`m.inkey()`**
いま押されているキーの名前を1つ返します。何も押されていなければ空文字です。

**`m.flush_keys()`**
押されている記録を全部捨てます。別のプログラムを呼んで戻ってきたときに
使います。

キーの名前は`UP` `DOWN` `LEFT` `RIGHT` `SPACE` `ENTER` `1` `2` `3` `4`
`Q` `BRK`です。`BRK`はESC。矢印はWASDでも入ります。

別のプログラムを呼んで戻ってきたら`m.flush_keys()`を呼んでください。呼ばないと、
向こうを終わらせたESCがそのまま自分の`pressed("BRK")`にも見えてしまいます。

### 音

**`m.beep(hz, ms=60, vol=0.5)`**
hzの高さの音をmsミリ秒鳴らします。`vol`は0〜1の大きさです。

実機の音は機械語でポート1本を反転させて作るので、出せるのは矩形波だけです。
半周期は待ちループの回数nで決まり、CPUが576kHzなので f = 36000/n になると
仮定しています。nが整数なので音程は飛び飛びですが、刻み幅は 1/n なので
低い音ほど細かくなります。指定した高さはいちばん近い n に丸められます。

| n | 音 | 隣との差 |
| --- | --- | --- |
| 4 | 9000Hz | 25% |
| 12 | 3000Hz | 8% |
| 36 | 1000Hz | 2.8% |
| 120 | 300Hz | 0.8% |
| 450 | 80Hz | 0.2% |

半音は約6%なので、中低音では音階が作れます。高い音になるほど狙った音程から
外れます。

ブザーは1つしかないので同時に鳴るのも1つです。新しい`beep`は前の音を止め
ます。鳴らしている間もプログラムは止まりません。

```python
m.beep(3600, 14, 0.22)     # ショットの短い音
m.beep(180, 420, 0.6)      # やられたときの低い音
```

### 残像

液晶の応答は`m.persist`で変えられます。1.0が実測の時定数どおり、0で残像
なしです。遊んでいる最中は`[`と`]`でも動かせます。プログラムから触るもの
ではありませんが、自分の描いたものが残像でどう見えるかを確かめるときに
使います。

### 時間

**`m.tick()`**
いまバッファに描いてあるものを液晶へ出し、次のフレームまで待って、
キーの状態を新しくします。窓が閉じられたら`False`を返すので`return`します。

**`m.wait(frames)`**
画面をそのままにしてframesフレーム待ちます。中で`m.tick()`を呼んでいるので、
窓が閉じられたら`False`を返します。

**`m.frame`**
起動からのフレーム数です。24で1秒。

**`m.seconds()`**
起動からの秒数です。

24fpsです。アニメーションが今どのコマかは、自前のカウンタを増やさなくても
`m.frame`を割れば出せます。下は3フレームごとに2枚を切り替える例です。

```python
m.sprite(x, y, HERO_A if (m.frame // 3) % 2 else HERO_B)
```

`m.frame`をsinに渡すと、行ったり来たりする動きが作れます。下は敵をふわふわ
上下させる例です。

```python
e["y"] += 0.22 * math.sin(m.frame * 0.14 + e["ph"])   # phは個体ごとの初期値
```

`0.14`がゆれる速さで、sinが1往復するのは2π/0.14でおよそ45フレーム、1.9秒
ほどです。毎フレーム足しこんでいるので、振れ幅は0.22/0.14でおよそ1.6ドット
になります。`e["ph"]`は敵1体ごとに変えておく初期値で、これがないと画面の
敵が全部そろって同じ高さで上下してしまいます。

### 定数

`m.WIDTH` = 120、`m.HEIGHT` = 7、`m.CELLS` = 24、`m.CELL_W` = 5、`m.FPS` = 24。

## 窓の座標設定について

`m.show(buf, top)`の使い方は「仮想画面(Buffer)」に書いたとおりです。ここでは
フレームごとの切り取る位置`top`の決め方について説明します。

SKY CAVEは縦24ドットの仮想画面に洞窟を描いて、そこを縦7ドットの窓で
見ています。

```python
cave = Buffer(24)
...
cave.cls()
draw_cave(cave, scroll)
m.show(cave, round(cam))    # camが窓の位置
```

窓の基準は自機にしてあります。上下に飛んだときに背景が動くので、画面より
大きいものを見ていることが伝わります。ただし追い方が素直すぎると自機がいつも
窓の中央に来てしまい、壁が窓の外に出たまま何を避けているのか分からなく
なります。ここでは係数0.22で遅れて追わせ、通路の高さを6ドットから12ドット
までうねらせて、どこかの壁が常に窓の中に残るようにしています。

## 横スクロール

バッファをずらす必要はありません。コース全体を通した座標(通し座標)を持って、
毎フレーム描き直すほうが速いですし簡単です。

```python
scroll += speed                 # floatでよい
sx = int(wx - scroll)           # 通し座標 wx → 画面のx
```

地形のように規則的なものは、配列を持たずにその場で計算できます。係数を変えれば
多重スクロールになります。

```python
if int(scroll * 0.32 + x) % 41 < 6:   # 遠くの雲。0.32倍で遅く流す
    m.px(x, 0)
```

## 当たり判定

描いたドットを読んで判定しないでください(`m.get()`は使わない)。数値で判定
します。残像や描画順に判定が引きずられると、原因の分からないバグになります。

縦7ドットしかないので、矩形どうしの厳密な判定はたいてい理不尽になります。
緩めてかまいません。JUMP HEROでは足が下から2ドット目まで降りているときだけ
当たり、SKY CAVEでは壁に1ドット食い込むまでは当たりにしないようにして
います。コインのような拾いものは矩形ではなく中心間の距離で見ています。

判定を緩めるときは、どちらの向きに緩めるかを物ごとに決めます。JUMP HEROの
天井の岩は地面の段と逆で、頭が上がりすぎたときだけ当たりにしてあります。
同じ当たりでも、跳ばせたい物と跳ばせたくない物では条件が反対になります。

絵のほうも1ドットあけておかないと判定が伝わりません。天井の岩は上2ドット
だけ描いて3ドット目を空けています。ここを埋めるとくぐる自機とくっついて
見え、通れたのか当たったのか分からなくなります。

## 画面より大きいキャラクタの扱い

SKY CAVEのボスは全高15ドットで、縦7ドットの窓には上半分か下半分しか
入りません。装甲はどこを撃っても弾を吸うだけで、当たるのは中央にひとつある
核だけです。核の高さに自機を合わせると、カメラもそこへ寄るので、狙って
いるあいだは体の中央しか見えません。上下の腕から弾が飛んでくるのは窓の
外なので、避けるには一度カメラを動かすことになります。砲口は96フレームの
うち40フレームだけ開きます。閉じているあいだは撃っても無駄なので、そこで
避けに専念することになります。

## 液晶の応答

点灯にも消灯にも数フレームかかります。速く動くドットは尾を引き、消したドット
もしばらく薄く残ります。これはマシン側のモデルで、バッファの中身は書いた
とおりになっています。表示だけの話なので判定には影響しません。

1フレームだけ点けて消すような表現は残像に埋もれて見えません。点滅は3〜5
フレーム単位を推奨します。

## 速度

1フレームぶんの描画はだいたい840ドットです。Pythonの素朴なループで書いても
24fpsには十分間に合います(液晶への転送は0.3ms/フレーム)。画面全体を
何度も塗り直すような書き方でも問題ありません。
